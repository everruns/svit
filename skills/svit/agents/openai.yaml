interface:
  display_name: "Svit"
  short_description: "Use Svit's transactional agent process runtime"
  default_prompt: "Use $svit to show a runnable transactional process example and explain the current sandbox boundary."
